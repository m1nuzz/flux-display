<UserControl
    x:Class="FluxDisplay.App.Controls.PresetCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource FluxCardBorderStyle}" Margin="0,0,0,12">
        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border Width="48" Height="48" CornerRadius="8" Background="#1AFFFFFF">
                <FontIcon Glyph="&#xE7F4;" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                    <Button
                        Padding="4"
                        Background="Transparent"
                        BorderThickness="0"
                        Command="{Binding ToggleTrayCommand}"
                        CommandParameter="{Binding}"
                        ToolTipService.ToolTip="Show in tray">
                        <Grid>
                            <FontIcon
                                Glyph="&#xE890;"
                                FontSize="16"
                                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding ShowInTray, Converter={StaticResource BoolToVisibilityConverter}}" />
                            <FontIcon
                                Glyph="&#xE7B3;"
                                FontSize="16"
                                Opacity="0.5"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding ShowInTray, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}" />
                        </Grid>
                    </Button>
                    <Border
                        Background="{StaticResource ActiveBadgeBrush}"
                        CornerRadius="12"
                        Padding="8,2"
                        Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Active" FontSize="11" />
                    </Border>
                </StackPanel>
                <TextBlock Text="{Binding MonitorName}" Style="{StaticResource FluxMutedTextStyle}" TextTrimming="CharacterEllipsis" MaxLines="1" />
                <TextBlock Text="{Binding ModeText}" Style="{StaticResource FluxMutedTextStyle}" TextTrimming="CharacterEllipsis" MaxLines="1" />
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button
                    Content="Apply"
                    Style="{StaticResource AccentButtonStyle}"
                    Command="{Binding ApplyCommand}"
                    CommandParameter="{Binding}" />
                <Button Content="⋯">
                    <Button.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="Edit" Command="{Binding EditCommand}" CommandParameter="{Binding}" />
                            <MenuFlyoutItem Text="Move up" Command="{Binding MoveUpCommand}" CommandParameter="{Binding}" />
                            <MenuFlyoutItem Text="Move down" Command="{Binding MoveDownCommand}" CommandParameter="{Binding}" />
                            <MenuFlyoutItem Text="Delete" Command="{Binding DeleteCommand}" CommandParameter="{Binding}" />
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
